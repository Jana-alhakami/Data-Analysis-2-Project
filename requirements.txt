# Requirements for running the Machine Learning Projects on Google Colab

pandas==1.5.3
scikit-learn==1.3.0
matplotlib==3.8.0
seaborn==0.12.2
mlxtend==0.22.0
wordcloud==1.9.2
plotly==5.15.0
apriori==1.0.4
association_rules==1.0.1

# Additional dependencies for Colab
# These might already be available in Google Colab, but it’s good to specify in case of compatibility issues
numpy==1.24.4
scipy==1.11.3

# If working with Google Drive for dataset storage
# You can add the following to mount Google Drive
# !pip install -U -q PyDrive
PyDrive==1.3.2
